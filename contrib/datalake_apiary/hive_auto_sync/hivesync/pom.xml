<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cloud.elastic.apiary</groupId>
        <artifactId>apiary-extensions-parent</artifactId>
        <version>7.3.8</version>
    </parent>

    <groupId>cloud.elastic.apiary</groupId>
    <artifactId>hivesync</artifactId>
    <version>1.0.0</version>

    <dependencies>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <!-- 42.7.2 already contains the CVE-2024-1597 fix; bump to the latest 42.7.x as hygiene / to avoid scanner boundary false positives -->
            <version>42.7.5</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
            <version>2.15.2</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j-impl</artifactId>
            <version>${log4j2.version}</version>
        </dependency>
        <!-- #381: provide org.apache.log4j.* via the PATCHED log4j2 1.x bridge. HMS 3.1.0 was
             written against log4j 1.x, but both providers (log4j:log4j and the bundled
             log4j-1.2-api) are excluded from hive-standalone-metastore below; re-add the bridge
             explicitly so any org.apache.log4j.* reference resolves without reintroducing
             vulnerable log4j 1.x. Same bridge datalake_agent gets transitively via
             spring-boot-starter-log4j2. -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-1.2-api</artifactId>
            <version>${log4j2.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.hive</groupId>
            <artifactId>hive-standalone-metastore</artifactId>
            <version>${hive.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.orc</groupId>
                    <artifactId>orc-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>jackson-databind</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.guava</groupId>
                    <artifactId>guava</artifactId> 
                </exclusion>
                <exclusion>
                    <groupId>org.apache.logging.log4j</groupId>
                    <artifactId>log4j-slf4j-impl</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.logging.log4j</groupId>
                    <artifactId>log4j-1.2-api</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-api</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.derby</groupId>
                    <artifactId>derby</artifactId>
                </exclusion>
                <!-- #580: legacy Log4j 1.x (pulled via metrics-reporter -> hadoop-common) -->
                <exclusion>
                    <groupId>log4j</groupId>
                    <artifactId>log4j</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-log4j12</artifactId>
                </exclusion>
                <!-- #381: drop the real Log4j 1.x impl (reload4j) and its SLF4J binding, both
                     pulled transitively via hadoop-common 2.10.2. reload4j exposes the same
                     org.apache.log4j.* classes as the log4j-1.2-api bridge declared above, so
                     leaving it in makes the active provider depend on fat-jar classpath ordering;
                     slf4j-reload4j is a second SLF4J binding competing with log4j-slf4j-impl.
                     Excluding both makes the log4j2 bridge the sole org.apache.log4j.* provider
                     and log4j-slf4j-impl the sole SLF4J binding, with no reliance on jar order. -->
                <exclusion>
                    <groupId>ch.qos.reload4j</groupId>
                    <artifactId>reload4j</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-reload4j</artifactId>
                </exclusion>
                <!-- CVE-2019-10172:Jackson 1.x(org.codehaus.jackson) -->
                <exclusion>
                    <groupId>org.codehaus.jackson</groupId>
                    <artifactId>jackson-core-asl</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.codehaus.jackson</groupId>
                    <artifactId>jackson-mapper-asl</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.codehaus.jackson</groupId>
                    <artifactId>jackson-jaxrs</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.codehaus.jackson</groupId>
                    <artifactId>jackson-xc</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- apiary -->
        <dependency>
            <groupId>org.jasypt</groupId>
            <artifactId>jasypt</artifactId>
            <version>1.9.3</version>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <version>1.12.1</version>
        </dependency>
        <dependency>
            <groupId>cloud.elastic.apiary</groupId>
            <artifactId>kafka-metastore-receiver</artifactId>
            <version>${apiary.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- #580/#381 guard: ban the real Log4j 1.x impls (log4j:log4j, ch.qos.reload4j:reload4j)
                 and reload4j's SLF4J binding (org.slf4j:slf4j-reload4j) from creeping back in
                 transitively via any path; org.apache.log4j.* must come only from the patched
                 log4j-1.2-api bridge, and log4j-slf4j-impl must stay the sole SLF4J binding. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.4.1</version>
                <executions>
                    <execution>
                        <id>ban-log4j1</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>log4j:log4j</exclude>
                                        <exclude>ch.qos.reload4j:reload4j</exclude>
                                        <exclude>org.slf4j:slf4j-reload4j</exclude>
                                    </excludes>
                                    <message>Legacy Log4j 1.x impls (log4j:log4j, ch.qos.reload4j:reload4j) and reload4j's SLF4J binding (org.slf4j:slf4j-reload4j) are excluded; org.apache.log4j.* must resolve via the patched log4j-1.2-api bridge only, with log4j-slf4j-impl as the sole SLF4J binding (see #580, #381).</message>
                                </bannedDependencies>
                            </rules>
                            <fail>true</fail>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>2.7.16</version>
                <configuration>
                    <mainClass>cloud.elastic.hivesync.kafka.consumer.HiveAutoSyncer</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
