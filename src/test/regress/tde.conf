# TDE regression test configuration.
# Used with: make check TEMP_CONFIG=tde.conf EXTRA_TESTS=tde_tablespace
#
# SECURITY WARNING: This passphrase is hard-coded for test use only.
# NEVER use this configuration in a production environment.

tde_kms_provider           = 'builtin'
tde_kms_builtin_passphrase = 'regression-test-key-do-not-use-in-production'
allow_in_place_tablespaces = on
